<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base Material3 theme -->
    <style name="AppTheme" parent="Theme.Material3.Light.NoActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:statusBarColor">@android:color/white</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:fitsSystemWindows">true</item>
        <item name="android:forceDarkAllowed">false</item>
    </style>

    <!-- Login/Onboarding theme - gradient background, light status bar -->
    <style name="AppTheme.Auth" parent="AppTheme">
        <item name="android:statusBarColor">@color/bg_teal_light</item>
    </style>

    <!-- Splash screen theme -->
    <!-- ═══ FIX: Use ic_logo (512x512) directly + icon background to avoid Android 12+ -->
    <!-- adaptive icon safe-zone clipping that cropped the bottom of the logo. -->
    <style name="AppTheme.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_bg</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/splash_icon</item>
        <item name="windowSplashScreenIconBackgroundColor">@color/splash_bg</item>
        <item name="postSplashScreenTheme">@style/AppTheme</item>
    </style>

    <!-- Toolbar title style -->
    <style name="ToolbarTitle">
        <item name="android:textSize">18sp</item>
        <item name="android:textStyle">bold</item>
    </style>

    <!-- Bottom Nav active indicator (Material3 pill) -->
    <style name="BottomNavIndicator" parent="Widget.Material3.BottomNavigationView.ActiveIndicator">
        <item name="android:color">@color/bg_teal_light</item>
        <item name="android:width">56dp</item>
        <item name="android:height">32dp</item>
        <item name="shapeAppearance">@style/BottomNavIndicatorShape</item>
    </style>

    <style name="BottomNavIndicatorShape" parent="ShapeAppearance.Material3.Corner.Full">
        <item name="cornerSize">16dp</item>
    </style>
</resources>
